@model List<SistemaGerenciadorBiblioteca.Models.LivrosModel>
@{
    ViewData["Title"] = "Listagem de Livros";
}


<div class="text-center">
    <h1 class="display-4">Lista de Livros</h1>
    <a class="btn btn-success" role="button" style="float: left" asp-controller="Livros" asp-action="Create">Adicionar Livro</a>
    <br />
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Título</th>
                <th scope="col">Autor</th>
                <th scope="col">Gênero Literário</th>
                <th scope="col">Editora</th>
                <th scope="col">Ano de Publicação</th>
                <th scope="col">Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var livro in Model)
            {

                <tr>
                    <td>@livro.Id_livro</td>
                    <td>@livro.Titulo</td>
                    <td>@livro.Autor</td>
                    <td>@livro.Genero</td>
                    <td>@livro.Editora</td>
                    <td>@livro.Ano</td>
                    <td>
                        <div class="btn-group" role="group" aria-label="Basic mixed styles example">
                            <a href="Livros/Edit/@livro.Id_livro" class="btn btn-warning">Editar</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
